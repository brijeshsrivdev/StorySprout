import Link from "next/link";
export default async function EditorPlaceholder({params,searchParams}:{params:Promise<{storyId:string}>;searchParams:Promise<{scene?:string}>}){const{storyId}=await params;const{scene}=await searchParams;return <main className="min-h-screen bg-slate-50 p-10 text-slate-950"><div className="mx-auto max-w-2xl rounded-3xl border border-slate-200 bg-white p-10 text-center shadow-sm"><p className="text-sm font-semibold tracking-wide text-indigo-600">EDITOR</p><h1 className="mt-3 text-3xl font-bold">Editor is next</h1><p className="mt-3 text-slate-600">Scene Setup is complete for this scene. The visual Editor is not implemented in this feature, so no Composition was created.</p>{scene&&<p className="mt-3 text-xs text-slate-400">Scene: {scene}</p>}<Link href={`/stories/${storyId}/scene-setup${scene?`?scene=${scene}`:""}`} className="mt-7 inline-flex rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Back to Scene Setup</Link></div></main>}
