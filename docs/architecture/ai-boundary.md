# AI boundary

AI is behind an application-level gateway. Future interfaces may include story generation, character generation, background generation and voice generation.

Provider SDKs are intentionally absent from the foundation. Provider-specific adapters should be introduced only when an actual integration is selected.

The contract is: AI proposes; the creator decides; the project/editor owns state.
